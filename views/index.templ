package views

templ IndexPage(viewObj *ViewObj) {
	@DefaultLayout(viewObj) {
    <div class="flex flex-col items-center justify-center flex-1 self-center">
        <h1 class="text-xl font-bold text-center mb-5">Welcome to Arthur</h1>
        @ButtonComponent("Login", "/login")
    </div>
	}
}
